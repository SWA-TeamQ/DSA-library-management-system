{
  "name": "dsa-library-management-system",
  "version": "0.1.0",
  "description": "Convenience scripts to build and run the CMake project with a single command",
  "private": true,
  "scripts": {
    "cmake:configure": "cmake -S . -B build",
    "cmake:build": "cmake --build build",
    "run": "build/lms.exe",
    "dev": "npm run cmake:configure && npm run cmake:build && npm run run"
  }
}
