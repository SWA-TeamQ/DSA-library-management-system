{
  "name": "dsa-library-management-system",
  "version": "0.1.0",
  "description": "Convenience scripts to build and run the CMake project with a single command",
  "private": true,
  "scripts": {
    "build" : "build.sh",
    "run": "lms.exe",
    "dev": "npm run build && npm run run"
  }
}
