{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch lms (built)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/lms.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,
            "preLaunchTask": "CMake Build",
            "MIMode": "gdb",
            "miDebuggerPath": "gdb"
        }
    ]
}
